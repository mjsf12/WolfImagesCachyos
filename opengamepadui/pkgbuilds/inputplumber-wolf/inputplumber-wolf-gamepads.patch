diff --git a/src/input/manager.rs b/src/input/manager.rs
index afc9f31..7ecf2ac 100644
--- a/src/input/manager.rs
+++ b/src/input/manager.rs
@@ -64,6 +64,11 @@ const VIRT_DEVICE_WHITELIST: &[&str] = &[
     "Sunshine X-Box One (virtual) pad",
     "Sunshine gamepad (virtual) motion sensors",
     "Sunshine Nintendo (virtual) pad",
+    "Wolf X-Box One (virtual) pad",
+    "Wolf PS5 (virtual) pad",
+    "Wolf DualSense (virtual) pad",
+    "Wolf gamepad (virtual) motion sensors",
+    "Wolf Nintendo (virtual) pad",
     "MSI WMI hotkeys",
 ];
 
diff --git a/src/input/source/evdev.rs b/src/input/source/evdev.rs
index 8ad3b05..e642e26 100644
--- a/src/input/source/evdev.rs
+++ b/src/input/source/evdev.rs
@@ -144,7 +144,8 @@ impl EventDevice {
                 Ok(Self::Blocked(source_device))
             }
             DriverType::Gamepad => {
-                let device = GamepadEventDevice::new(device_info.clone(), capability_map)?;
+                let device =
+                    GamepadEventDevice::new(device_info.clone(), &conf, capability_map)?;
                 let source_device =
                     SourceDriver::new(composite_device, device, device_info.into(), conf);
                 Ok(Self::Gamepad(source_device))
diff --git a/src/input/source/evdev/gamepad.rs b/src/input/source/evdev/gamepad.rs
index 1204e55..c84721d 100644
--- a/src/input/source/evdev/gamepad.rs
+++ b/src/input/source/evdev/gamepad.rs
@@ -17,6 +17,7 @@ use crate::drivers::steam_deck::hid_report::{
 use crate::input::event::evdev::translator::EventTranslator;
 use crate::input::output_capability::{Haptic, OutputCapability, LED};
 use crate::{
+    config::SourceDevice,
     drivers::dualsense::hid_report::SetStatePackedOutputData,
     input::{
         capability::{Capability, Gamepad, GamepadAxis, GamepadButton},
@@ -43,12 +44,19 @@ impl GamepadEventDevice {
     /// Create a new [Gamepad] source device from the given udev info
     pub fn new(
         device_info: UdevDevice,
+        config: &Option<SourceDevice>,
         capability_map: Option<CapabilityMapConfigV2>,
     ) -> Result<Self, Box<dyn Error + Send + Sync>> {
         let path = device_info.devnode();
         log::debug!("Opening device at: {}", path);
         let mut device = Device::open(path.clone())?;
-        device.grab()?;
+
+        // Wolf keeps its source gamepad visible to games in the same isolated
+        // container. The D-Bus target still forwards its events to OpenGamepadUI.
+        let should_passthru = config.as_ref().and_then(|c| c.passthrough).unwrap_or(false);
+        if !should_passthru {
+            device.grab()?;
+        }
 
         // Set the device to do non-blocking reads
         // TODO: use epoll to wake up when data is available
diff --git a/src/input/composite_device/mod.rs b/src/input/composite_device/mod.rs
index fd2fdd7..733aa59 100644
--- a/src/input/composite_device/mod.rs
+++ b/src/input/composite_device/mod.rs
@@ -937 +937,7 @@ impl CompositeDevice {
-        let intercept = self.intercept_mode == InterceptMode::Pass;
+        // Multi-button activation chords emulate a button that the physical
+        // controller does not have (for example Start + Select as Guide).
+        // Keep recognizing those chords after the frontend has entered
+        // Always mode so they also work from OpenGamepadUI's own menus.
+        let intercept = self.intercept_mode == InterceptMode::Pass
+            || (self.intercept_mode == InterceptMode::Always
+                && self.intercept_activation_caps.len() > 1);
@@ -1028 +1034,7 @@ impl CompositeDevice {
-        if self.intercept_active_inputs.is_empty() && cap == first_cap {
+        let guide_capability = Capability::Gamepad(Gamepad::Button(GamepadButton::Guide));
+        let emulates_missing_guide = self.intercept_mode_target_cap == guide_capability
+            && !self.intercept_activation_caps.contains(&guide_capability);
+        if self.intercept_active_inputs.is_empty()
+            && (cap == first_cap
+                || (emulates_missing_guide && self.intercept_activation_caps.contains(cap)))
+        {
@@ -2050,33 +2062,42 @@ impl CompositeDevice {
                 // We must have a match, we are of the correct length and all capabilities matched.
                 log::debug!("Found activation chord!");
-                for c in self.intercept_activation_caps.clone() {
-                    if self.active_inputs.contains(&c) {
-                        log::trace!("Removed inactive capability: {c:?}");
-                        let index = self.active_inputs.iter().position(|r| r == &c).unwrap();
-                        self.active_inputs.remove(index);
-                    }
-                }
-                self.intercept_active_inputs.clear();
-
                 self.set_intercept_mode(InterceptMode::Always).await;
-                // Generate a new chord
+                // Keep the synthetic target pressed until one of the physical
+                // activation buttons is released. This lets consumers handle
+                // follow-up combinations such as Guide + A or Guide + X.
                 let event = NativeEvent::new(
                     self.intercept_mode_target_cap.clone(),
                     InputValue::Bool(true),
                 );
-                let event2 = NativeEvent::new(
-                    self.intercept_mode_target_cap.clone(),
-                    InputValue::Bool(false),
-                );
-                let chord: Vec<NativeEvent> = vec![event, event2];
+                let chord: Vec<NativeEvent> = vec![event];
                 log::trace!("Release new chord: {chord:?}");
                 self.write_chord_events(chord).await?;
                 return Ok(true);
             } else if !is_pressed {
                 log::debug!("It is an UP event!");
-                // We only had a partial match and one of those events is released,
-                // release it
                 if self.intercept_active_inputs.contains(&cap) {
+                    // A complete activation chord is represented by the
+                    // synthetic target until the first trigger is released.
+                    if self.intercept_mode == InterceptMode::Always
+                        && self.intercept_active_inputs.len()
+                            == self.intercept_activation_caps.len()
+                    {
+                        for c in self.intercept_activation_caps.clone() {
+                            if let Some(index) = self.active_inputs.iter().position(|r| r == &c) {
+                                self.active_inputs.remove(index);
+                            }
+                        }
+                        self.intercept_active_inputs.clear();
+                        let event = NativeEvent::new(
+                            self.intercept_mode_target_cap.clone(),
+                            InputValue::Bool(false),
+                        );
+                        self.write_chord_events(vec![event]).await?;
+                        return Ok(true);
+                    }
+
+                    // We only had a partial match and one of those events is
+                    // released, so replay the original button as a click.
                     let index = self
                         .intercept_active_inputs
                         .iter()
@@ -2090,7 +2111,10 @@ impl CompositeDevice {
                     return Ok(true);
                 }
             }
-        } else if !self.intercept_active_inputs.is_empty() && is_pressed {
+        } else if self.intercept_mode == InterceptMode::Pass
+            && !self.intercept_active_inputs.is_empty()
+            && is_pressed
+        {
             // Handle chords with partial matches. Up events will be handled normally.
             log::debug!("This event is not what we're looking for.");
             self.intercept_active_inputs.push(cap);
